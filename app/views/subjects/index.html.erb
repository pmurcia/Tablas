<ul>
<% @subjects.each do |subject| %>
  <li><%= link_to(subject.name, subject_path(subject)) %> <%= link_to('Edit', edit_subject_path(subject)) %> <%= link_to('Delete', subject_path(subject), method: :delete) %></li>
<% end %>
</ul>

<%= button_to('Nueva asignatura', new_subject_path, method: :get) %>