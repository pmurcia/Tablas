<ul>
<% @subjects.each do |subject| %>
  <li><%= link_to(subject.name, subject_path(subject)) %> <%= link_to('Edit', edit_subject_path(subject)) %></li>
<% end %>
</ul>

<%= button_to('Nueva asignatura', new_subject_path, method: :get) %>